#'@title A trained model
#'@description An already trained ML model (including trainData & testData) with the "lm" method using the default data from the package, to be used in the predict example{, and potentially by the user for further predictions}.

"model"
